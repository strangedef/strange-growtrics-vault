- char_start, char_end is redundant
- C2. StructuralClaim IDs aren't declared stable across versions.
- How should a 429 be treated
- Yes for AuthProfile's copy. No for ResourceInference's.

Why AuthProfile.identity_slots was always removable: it's derived, not truth. Truth is roles[] + credentials{} on the same entity. A slot is just the arithmetic —