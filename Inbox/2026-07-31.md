give me the plan that my techlead can understand, don't put too much internal details in the title, the task description.
POST /orders requires product_id; POST /products requires category_id. Those requires edges form a graph:

category ← product ← order