**Y:**

- Designed Settled run lock / concurrency, webhook, SuiteRun triggers
- Decided pass 2 (cross-chunk reconciliation) handles large APIs by grouping endpoints rather than reconciling globally
- Reviewed the API testing capability spec and build plan
**T:**
- Consolidate into a single synthesized technical design
- Break down API Testing USPs

### Unresolved
~~Create Order between objects~~

### Stories
#: 1
Story: Register target API with environment and bearer/api_key credential over REST
Vertical slice: Scaffold (FastAPI, compose Postgres, Alembic, CI) + Target/Environment/Credential models, migration, CRUD endpoints, typed-config
validation, tests
Done when: curl registers target + environment + credential; invalid config rejected; pytest green
────────────────────────────────────────
#: 2
Story: Point at spec URL (or upload raw spec), get versioned canonical endpoint list
Vertical slice: SpecArtifact model + migration, POST /targets/{id}/spec with spec_url fetch or raw JSON/YAML body, prance normalization, Swagger2
conversion, content-hash no-op, fixture tests (petstore + edge cases)
Done when: /openapi.json URL → SpecArtifact v1 resolved endpoints; unchanged re-ingest = same version; bad spec / failed fetch = 422, nothing
persisted
────────────────────────────────────────
#: 3
Story: LLM classifies which POST endpoints create resources, spec validates the claim
Vertical slice: LLM client wiring + API key config, per-POST classification call with structured output (is_create, resource_name, paired_item_path,
rationale), deterministic spec validation of pairing, per-endpoint failure to generation_notes, recorded-fixture mocks for CI
Done when: Petstore analysis marks POST /orders create paired to /orders/{id}, rejects POST /orders/{id}/cancel; invalid pairing dropped; CI green
with zero live LLM calls
────────────────────────────────────────
#: 4
Story: Spec ingest auto-generates versioned suite of functional, schema, and create cases — every assertion citing its source
Vertical slice: TestSuite/TestCase models + migration, functional + schema builders, create builder consuming analysis (body synthesis, ID capture,
one-hop depends_on), atomic suite assembly on ingest, source refs incl. create_analysis kind, fixture tests
Done when: Ingest yields TestSuite v1: case per GET/HEAD + per-leaf schema asserts + create cases; item-read case depends on create capture, not
needs_seed_value; every assertion resolves to spec path or analysis rationale
────────────────────────────────────────
#: 5
Story: Run suite against chosen environment and read per-run JSON report — proven end-to-end on demo orders API
Vertical slice: SuiteRun/RunResult models + migration, httpx runner (credential injection, concurrency 4 / 10 rps, timeout + one retry, 429-as-error,
creates-first ordering, capture filling, request dedup), report endpoint, demo orders API fixture, e2e script
Done when: One script: register → spec URL → run → report; creates run first, seeded reads pass on empty API; report totals per category/outcome +
expected-vs-actual; 429/timeout/retry green under mocked HTTP

Sequence strict 1→5; each story = own migration + demoable increment, session-sized (story 4 likely two sessions: read builders, then create builder + assembly).

